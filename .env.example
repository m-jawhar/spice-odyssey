# Flask session secret (required in production)
SECRET_KEY=replace-with-a-long-random-secret

# Optional: Firebase Admin service account JSON as one line
# FIREBASE_CREDENTIALS={"type":"service_account",...}
